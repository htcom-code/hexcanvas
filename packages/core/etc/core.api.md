## API Report File for "@hexcanvas/core"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public
export function abortError(message?: string): Error;

// @public (undocumented)
export abstract class AbstractByteSource implements ByteSource {
    protected currentVersion: number;
    // (undocumented)
    abstract ensure(offset: number, length: number, signal?: AbortSignal): Promise<void>;
    // (undocumented)
    abstract readonly length: number;
    protected notify(changes: ChangeSet): void;
    // (undocumented)
    abstract peek(offset: number, length: number, out?: Uint8Array): Uint8Array | undefined;
    // (undocumented)
    subscribe(listener: ByteSourceListener): () => void;
    // (undocumented)
    get version(): number;
}

// @public
export function addressDigitsFor(byteLength: number, radix: AddressRadix, minimum?: number): number;

// @public
export type AddressRadix = "hex" | "decimal";

// @public
export function alignedRowPlans(differences: readonly HexDifference[], leftLength: number, rightLength: number, bytesPerRow: number): {
    left: RowPlan;
    right: RowPlan;
} | undefined;

// @public
export interface AnchoredOptions {
    // (undocumented)
    blockSize?: number;
    // (undocumented)
    limit?: number;
    // (undocumented)
    maxDistance?: number;
    // (undocumented)
    maxGap?: number;
    // (undocumented)
    signal?: AbortSignal;
}

// @public
export const asciiPrintable: PrintableChar;

// @public
export interface BinaryBuffer {
    // (undocumented)
    readonly length: number;
    // (undocumented)
    read(offset: number, length: number): Uint8Array;
    // (undocumented)
    write(offset: number, bytes: Uint8Array): void;
}

// @public
export interface Binding {
    alt: boolean;
    // (undocumented)
    code?: string;
    ctrl: boolean;
    key?: string;
    meta: boolean;
    mod: boolean;
    shift: boolean;
}

// @public
export const bookmarkKind = "bookmark";

// @public
export function byPaintOrder(left: DecorationInput, right: DecorationInput): number;

// @public
export type ByteGroupSize = 1 | 2 | 4 | 8;

// @public
export interface ByteSelection {
    end: number;
    start: number;
}

// @public
export interface ByteSource {
    // (undocumented)
    apply?(changes: ChangeSet): void;
    ensure(offset: number, length: number, signal?: AbortSignal): Promise<void>;
    // (undocumented)
    readonly length: number;
    peek(offset: number, length: number, out?: Uint8Array): Uint8Array | undefined;
    save?(): AsyncIterable<Uint8Array>;
    subscribe(listener: ByteSourceListener): () => void;
    readonly version: number;
}

// @public
export type ByteSourceListener = (changes: ChangeSet) => void;

// @public
export interface Change {
    from: number;
    insert: Uint8Array;
    to: number;
}

// @public
export class ChangeHistory {
    constructor(options?: ChangeHistoryOptions);
    get canRedo(): boolean;
    get canUndo(): boolean;
    clear(): void;
    get depth(): number;
    push(source: ByteSource, changes: ChangeSet, coalesce?: boolean): boolean;
    redo(source: ByteSource): ChangeSet | undefined;
    get stateId(): number;
    undo(source: ByteSource): ChangeSet | undefined;
}

// @public
export interface ChangeHistoryOptions {
    coalesceWindow?: number;
    // (undocumented)
    now?: () => number;
}

// @public
export class ChangeSet {
    constructor(changes?: readonly Change[]);
    readonly changes: readonly Change[];
    static empty(): ChangeSet;
    static insert(at: number, insert: Uint8Array): ChangeSet;
    get isEmpty(): boolean;
    get lengthDelta(): number;
    mapPos(pos: number, assoc?: -1 | 1): number;
    static remove(from: number, to: number): ChangeSet;
    static replace(from: number, to: number, insert?: Uint8Array): ChangeSet;
}

// @public
export const clampOffset: (offset: number, length: number) => number;

// @public
export interface Command {
    // (undocumented)
    defaultKeys: readonly string[];
    // (undocumented)
    id: CommandId;
    label: string;
}

// @public
export function commandFor(lookup: readonly KeyLookupEntry[], input: KeyInput, platform: Platform): CommandId | undefined;

// @public
export type CommandId = "find" | "findNext" | "findPrevious" | "replace" | "goto" | "toggleBookmark" | "nextBookmark" | "previousBookmark" | "nextDifference" | "previousDifference" | "switchColumn" | "readRow" | "readRegion";

// @public
export function commands(platform?: Platform, text?: HexText): readonly Command[];

// @public
export function compareAligned(left: ByteSource, right: ByteSource, options?: DiffOptions): Promise<HexDifference[]>;

// @public
export function compareAnchored(left: ByteSource, right: ByteSource, options?: AnchoredOptions): Promise<HexDifference[]>;

// @public
export function compareEditScript(left: ByteSource, right: ByteSource, options?: EditScriptOptions): Promise<HexDifference[]>;

// @public
export const cp437Printable: PrintableChar;

// @public
export function createAlignedDiffProvider(options?: Omit<DiffOptions, "limit" | "signal">): DiffProvider;

// @public
export function createAnchoredDiffProvider(options?: Omit<AnchoredOptions, "limit" | "signal">): DiffProvider;

// @public
export function createByteSearchProvider(options?: SearchOptions): SearchProvider;

// @public
export function createEditScriptDiffProvider(options?: Omit<EditScriptOptions, "limit" | "signal">): DiffProvider;

// @public
export function createLayout(options: LayoutOptions): HexLayout;

// @public
export function createScrollScale(totalRows: number, rowHeight: number, options?: ScrollScaleOptions): {
    height: number;
    toLogical(scrollTop: number): number;
    toRow(scrollTop: number): number;
    toScrollTop(row: number): number;
    fromLogical(logical: number): number;
};

// @public
export interface Cursor {
    // (undocumented)
    column: "hex" | "ascii";
    nibble: 0 | 1;
    offset: number;
}

// @public
export const darkTheme: HexTheme;

// @public
export interface Decoration {
    color?: string;
    end: number;
    // (undocumented)
    id: string;
    kind?: string;
    // (undocumented)
    label?: string;
    labelVisible?: boolean;
    opacity?: number;
    priority?: number;
    start: number;
    textColor?: string;
}

// @public
export type DecorationInput = Omit<Decoration, "id"> & {
    id?: string;
};

// @public
export interface DecorationQuery {
    between(from: number, to: number): readonly DecorationInput[];
}

// @public
export class DecorationStore implements DecorationQuery {
    add(decoration: DecorationInput): Decoration;
    addAll(decorations: readonly DecorationInput[]): number;
    get all(): readonly Decoration[];
    allAt(offset: number, kind?: string): Decoration[];
    at(offset: number, kind?: string): Decoration | undefined;
    between(from: number, to: number): Decoration[];
    clear(kind?: string): void;
    countOfKind(kind: string): number;
    get hasVisibleLabels(): boolean;
    map(changes: ChangeSet): boolean;
    ofKind(kind: string): Decoration[];
    ordinalOfKindAt(kind: string, offset: number): number;
    remove(id: string): boolean;
    replace(decorations: readonly DecorationInput[], kind?: string): number;
    get size(): number;
}

// @public
export const defaultFont = "13px ui-monospace, SFMono-Regular, Menlo, monospace";

// @public
export const defaultText: HexText;

// @public
export function detectPlatform(): Platform | undefined;

// @public
export const diffDeleteKind = "diff-delete";

// @public
export const diffInsertKind = "diff-insert";

// @public
export type DiffKind = "replace" | "insert" | "delete";

// @public
export const diffKinds: Readonly<Record<DiffKind, string>>;

// @public
export const diffLimit = 10000;

// @public
export interface DiffOptions {
    // (undocumented)
    chunkSize?: number;
    limit?: number;
    // (undocumented)
    readAhead?: number;
    // (undocumented)
    signal?: AbortSignal;
}

// @public
export interface DiffProvider {
    // (undocumented)
    compare(left: ByteSource, right: ByteSource, options: {
        limit: number;
        signal?: AbortSignal;
    }): Promise<HexDifference[]>;
}

// @public
export const diffReplaceKind = "diff-replace";

// @public
export type EditMode = "read-only" | "overwrite" | "insert";

// @public
export interface EditScriptOptions extends DiffOptions {
    maxBytes?: number;
    maxDistance?: number;
}

// @public
export function encodeQuery(query: string, mode: string): Uint8Array;

// @public
export type FeatureMode = "off" | "native" | "custom";

// @public
export function findAll(source: ByteSource, query: Uint8Array, limit?: number, options?: SearchOptions): Promise<SearchMatch[]>;

// @public
export function findNext(source: ByteSource, query: Uint8Array, from?: number, options?: SearchOptions | number): Promise<SearchMatch | undefined>;

// @public
export function findPrevious(source: ByteSource, query: Uint8Array, before?: number, options?: SearchOptions | number): Promise<SearchMatch | undefined>;

// @public
export const fontProperty = "--hexcanvas-font";

// @public
export function formatBinding(text: string, platform: Platform): string;

// @public
export function fromBinaryBuffer(buffer: BinaryBuffer): ByteSource;

// @public
export function getVisibleRows(byteLength: number, options: ViewportOptions): VisibleRows;

// @public
export function hasUniformAdvance(measure: (text: string) => number, font: string, charWidth: number,
printableChars?: readonly string[]): boolean;

// @public
export class HexCanvasRenderer {
    // (undocumented)
    render(canvas: HTMLCanvasElement, request: RenderRequest): void;
}

// @public
export class HexCompare {
    constructor(options: HexCompareOptions);
    clear(): void;
    compare(force?: boolean): Promise<number>;
    destroy(): void;
    get differences(): readonly HexDifference[];
    // (undocumented)
    getState: () => HexCompareState;
    nextDifference(): boolean;
    // (undocumented)
    previousDifference(): boolean;
    // (undocumented)
    subscribe: (listener: () => void) => (() => void);
}

// @public
export interface HexCompareOptions {
    // (undocumented)
    left: HexEngine;
    limit?: number;
    provider?: DiffProvider;
    // (undocumented)
    right: HexEngine;
    syncScroll?: boolean;
}

// @public
export interface HexCompareState {
    compared: boolean;
    comparing: boolean;
    // (undocumented)
    differenceCount: number;
    differenceIndex: number;
    differenceTruncated: boolean;
    // (undocumented)
    error: string | undefined;
    stale: boolean;
}

// @public
export interface HexDifference {
    // (undocumented)
    kind: DiffKind;
    // (undocumented)
    left: ByteSelection;
    // (undocumented)
    right: ByteSelection;
}

// @public
export interface HexDisplayOptions {
    decorationLabels?: boolean;
    decorationOpacity?: number;
    highlightCursorAddress?: boolean;
    highlightCursorAscii?: boolean;
    printableChars?: readonly string[];
}

// @public
export class HexEngine {
    constructor(options: HexEngineOptions);
    addDecoration(decoration: DecorationInput): Decoration;
    addDecorations(decorations: readonly DecorationInput[]): number;
    adoptStyles(element: Element, options?: {
        remeasureFont?: boolean;
    }): void;
    announce(text: string): void;
    get bookmarkCount(): number;
    bookmarks(): Decoration[];
    get byteSource(): ByteSource;
    clearDecorations(kind?: string): void;
    clearMatches(): void;
    clearSelection(): void;
    closeGoto(): void;
    closeReplace(): void;
    closeSearch(): void;
    copySelection(): string;
    cut(): void;
    decorationCount(kind?: string): number;
    get decorations(): readonly Decoration[];
    decorationsAt(offset: number, kind?: string): DecorationInput[];
    decorationsBetween(from: number, to: number, kind?: string): DecorationInput[];
    deleteBackward(): boolean;
    deleteForward(): boolean;
    deleteSelection(): boolean;
    describeCursor(): string;
    describeRegion(): string;
    describeRow(row?: number): string;
    destroy(): void;
    get dirtyRanges(): readonly ByteSelection[];
    get editable(): boolean;
    ensureRead(offset: number, length: number): Promise<void>;
    extendSelectionTo(offset: number): void;
    findAllMatches(force?: boolean): Promise<number>;
    getState: () => HexEngineState;
    gotoAddress(text: string): boolean;
    gotoOffset(offset: number): void;
    handleKey(input: KeyInput): boolean;
    hitTest(x: number, y: number): {
        offset: number;
        column: Cursor["column"];
        region: "address" | "hex" | "ascii";
    };
    get hostedKinds(): readonly string[];
    insertBytes(bytes: Uint8Array, at?: number, coalesce?: boolean): void;
    invalidateDecorations(_kind?: string): void;
    get isDragging(): boolean;
    get keybindings(): readonly Keybinding[];
    keyFor(id: CommandId): string | undefined;
    get keyPlatform(): Platform;
    get layout(): HexLayout;
    get lengthUnchanged(): boolean;
    get logicalScrollTop(): number;
    markSaved(): void;
    get matches(): Decoration[];
    get maxScrollLeft(): number;
    moveCursor(offset: number, column?: "ascii" | "hex", nibble?: 0 | 1): void;
    nextBookmark(): boolean;
    openGoto(): void;
    openReplace(): void;
    openSearch(): void;
    paste(text: string): boolean;
    get plan(): RowPlan;
    pointerDown(x: number, y: number): void;
    pointerMove(x: number, y: number, buttonsHeld?: boolean): void;
    pointerUp(): void;
    previousBookmark(): boolean;
    read(offset: number, length: number): Uint8Array | undefined;
    get readOnly(): boolean;
    redo(): boolean;
    refreshTheme(options?: {
        remeasureFont?: boolean;
    }): void;
    removeDecoration(id: string): boolean;
    render(canvas: HTMLCanvasElement): void;
    replace(): Promise<boolean>;
    replaceAll(): Promise<number>;
    runCommand(id: CommandId): boolean;
    runGoto(): void;
    runSearch(direction?: SearchDirection): Promise<void>;
    save(): AsyncIterable<Uint8Array> | undefined;
    savePatch(): AsyncGenerator<{
        offset: number;
        bytes: Uint8Array;
    }> | undefined;
    get scrollHeight(): number;
    scrollToOffset(offset: number): void;
    scrollToRow(row: number): void;
    get scrollWidth(): number;
    select(start: number, end: number, cursorAt?: "start" | "end" | "keep"): void;
    selectAll(): void;
    selectionText(format?: "hex" | "text"): string;
    setCommandHandler(id: CommandId, handler: (() => boolean) | undefined): void;
    setDecorations(decorations: readonly DecorationInput[], kind?: string): number;
    setDecorationSource(kind: string, source: DecorationQuery | undefined): void;
    setEditMode(editMode: EditMode): void;
    setGotoQuery(gotoQuery: string): void;
    setOptions(options: Omit<HexEngineOptions, "source">): void;
    setReplaceQuery(replaceQuery: string): void;
    setRowPlan(plan: RowPlan | undefined): void;
    setScrollLeft(scrollLeft: number): void;
    setScrollTop(scrollTop: number): void;
    setSearchMode(searchMode: SearchMode): void;
    setSearchQuery(searchQuery: string): void;
    setSource(source: ByteSource | BinaryBuffer): void;
    setViewportSize(width: number, height: number): void;
    subscribe: (listener: () => void) => (() => void);
    get text(): HexText;
    toggleBookmark(offset?: number, label?: string): boolean;
    get totalRows(): number;
    undo(): boolean;
    get visibleRows(): VisibleRows;
    writeByte(offset: number, value: number): void;
}

// @public
export interface HexEngineOptions {
    addressRadix?: AddressRadix;
    asciiColumn?: boolean;
    byteGroup?: ByteGroupSize;
    bytesPerRow?: number;
    display?: HexDisplayOptions;
    editMode?: EditMode;
    font?: string;
    goto?: FeatureMode;
    keymap?: Keymap;
    labelWidth?: LabelWidth;
    onChange?: (changes: ChangeSet) => void;
    onCopy?: (text: string) => void;
    onCursorChange?: (cursor: Cursor) => void;
    onSearchRequest?: (request: SearchRequest) => void;
    onSelectionChange?: (selection: ByteSelection | undefined) => void;
    platform?: Platform;
    printable?: PrintableChar;
    replace?: FeatureMode;
    rowHeight?: number;
    search?: FeatureMode;
    searchMatchLimit?: number;
    searchModes?: readonly SearchMode[];
    searchPriority?: number;
    searchProvider?: SearchProvider;
    source: ByteSource | BinaryBuffer;
    spacing?: HexSpacing;
    text?: HexTextOverrides;
    theme?: HexTheme;
}

// @public
export interface HexEngineState {
    announcement: {
        text: string;
        serial: number;
    } | undefined;
    canRedo: boolean;
    canUndo: boolean;
    cursor: Cursor;
    decorationRevision: number;
    editMode: EditMode;
    gotoError: string | undefined;
    gotoFeature: FeatureMode;
    gotoOpen: boolean;
    gotoQuery: string;
    replaceFailed: boolean;
    replaceFeature: FeatureMode;
    replaceMessage: string | undefined;
    replaceOpen: boolean;
    replaceQuery: string;
    revision: number;
    scrollLeft: number;
    scrollTop: number;
    searchError: string | undefined;
    searchFeature: FeatureMode;
    searching: boolean;
    searchMatchCount: number;
    searchMatchIndex: number;
    searchMode: SearchMode;
    searchModes: readonly SearchMode[];
    searchOpen: boolean;
    searchQuery: string;
    searchTruncated: boolean;
    selection: ByteSelection | undefined;
    viewportHeight: number;
    viewportWidth: number;
}

// @public
export interface HexHit {
    // (undocumented)
    index: number;
    // (undocumented)
    region: "address" | "hex" | "ascii";
}

// @public
export interface HexLayout {
    readonly addressDigits: number;
    readonly addressRadix: AddressRadix;
    readonly addressWidth: number;
    readonly addressX: number;
    readonly asciiColumn: boolean;
    readonly asciiStart: number;
    asciiX(index: number): number;
    readonly byteGroup: ByteGroupSize;
    readonly bytesPerRow: number;
    byteX(index: number): number;
    readonly charWidth: number;
    readonly font: string;
    formatAddress(offset: number): string;
    readonly hexStart: number;
    hitTest(x: number): HexHit;
    readonly labelStart: number;
    readonly labelWidth: number;
    nibbleX(index: number, nibble: 0 | 1): number;
    readonly spacing: ResolvedSpacing;
    readonly width: number;
}

// @public
export interface HexSpacing {
    addressPaddingLeft?: number;
    addressPaddingRight?: number;
    byteGap?: number;
    columnGutter?: number;
    labelGutter?: number;
    minimumAddressDigits?: number;
}

// @public
export interface HexText {
    // (undocumented)
    closeCompareButton: string;
    // (undocumented)
    closeGotoButton: string;
    // (undocumented)
    closeReplaceButton: string;
    // (undocumented)
    closeSearchButton: string;
    commands: Record<CommandId, string>;
    // (undocumented)
    compareButton: string;
    comparing: string;
    comparisonStale: string;
    cursorByte: (hex: string) => string;
    cursorByteNotLoaded: string;
    cursorCharacter: (character: string) => string;
    cursorEditMode: (mode: string, describedCursor: string) => string;
    cursorHexColumn: string;
    cursorSelection: (bytes: number) => string;
    cursorTextColumn: string;
    deletedLabel: (bytes: number) => string;
    differencePosition: (index: number, count: number, truncated: boolean) => string;
    emptyQuery: string;
    findHexField: string;
    findHexPlaceholder: string;
    // (undocumented)
    findNextButton: string;
    // (undocumented)
    findPreviousButton: string;
    findTextField: string;
    findTextPlaceholder: string;
    // (undocumented)
    gotoButton: string;
    gotoField: string;
    gotoPlaceholder: string;
    gotoRow: string;
    identical: string;
    insertedLabel: (bytes: number) => string;
    invalidQuery: string;
    invalidReplacement: string;
    leaveWithTab: string;
    leftPane: string;
    // (undocumented)
    nextDifferenceButton: string;
    noMatch: string;
    notAnAddress: string;
    notEditable: string;
    nothingToRead: string;
    // (undocumented)
    previousDifferenceButton: string;
    regionDecoration: (label: string, from: string, to: string) => string;
    regionSelection: (from: string, to: string, bytes: number, hex: string, truncated: boolean) => string;
    // (undocumented)
    replaceAllButton: string;
    // (undocumented)
    replaceButton: string;
    replaced: (count: number) => string;
    replacedLabel: (bytes: number) => string;
    replacedTruncated: (count: number, limit: number) => string;
    // (undocumented)
    replaceField: string;
    // (undocumented)
    replacePlaceholder: string;
    replaceRow: string;
    rightPane: string;
    rowDescription: (address: string, hex: string, text: string) => string;
    rowGap: string;
    rowNotLoaded: (address: string) => string;
    searchModeField: string;
    searchPanel: string;
    searchPosition: (index: number, count: number, truncated: boolean) => string;
    searchTotal: (count: number, truncated: boolean) => string;
    splitHandle: string;
    // (undocumented)
    toggleReplaceButton: string;
    unnamedRegion: string;
}

// @public
export type HexTextOverrides = Partial<Omit<HexText, "commands">> & {
    commands?: Partial<Record<CommandId, string>>;
};

// @public
export interface HexTheme {
    background: string;
    caret: string;
    cursorByte: string;
    cursorByteText: string;
    cursorRow: string;
    decoration: string;
    decorationLabel: string;
    diffDelete: string;
    diffInsert: string;
    diffReplace: string;
    foreground: string;
    muted: string;
    searchMatch: string;
    selection: string;
}

// @public
export interface HistoryEntry {
    // (undocumented)
    at: number;
    changes: ChangeSet;
    inverse: ChangeSet;
    state: number;
}

// @public
export interface Interval {
    // (undocumented)
    end: number;
    // (undocumented)
    start: number;
}

// @public
export class IntervalColumns {
    constructor(starts: Float64Array, ends: Float64Array, count: number);
    // (undocumented)
    covering(offset: number, out: number[]): number[];
    overlapping(from: number, to: number, out: number[]): number[];
}

// @public
export class IntervalIndex<T extends Interval> {
    constructor(items: readonly T[]);
    covering(offset: number): T[];
    overlapping(from: number, to: number): T[];
}

// @public
export function invalidateFontMetrics(font?: string): void;

// @public
export const isAbortError: (error: unknown) => boolean;

// @public
export function isByteSource(value: BinaryBuffer | ByteSource): value is ByteSource;

// @public
export interface Keybinding {
    // (undocumented)
    id: CommandId;
    // (undocumented)
    keys: readonly string[];
    // (undocumented)
    label: string;
}

// @public
export interface KeyInput {
    // (undocumented)
    altKey?: boolean;
    code?: string;
    // (undocumented)
    ctrlKey?: boolean;
    // (undocumented)
    key: string;
    // (undocumented)
    metaKey?: boolean;
    // (undocumented)
    shiftKey?: boolean;
}

// @public
export interface KeyLookupEntry {
    // (undocumented)
    binding: Binding;
    // (undocumented)
    id: CommandId;
}

// @public
export type Keymap = Partial<Record<CommandId, string | readonly string[] | null>>;

// @public
export type LabelWidth = 16 | 32 | 64;

// @public
export const latin1Printable: PrintableChar;

// @public
export interface LayoutOptions {
    // (undocumented)
    addressRadix?: AddressRadix;
    asciiColumn?: boolean;
    // (undocumented)
    byteGroup?: ByteGroupSize;
    byteLength: number;
    // (undocumented)
    bytesPerRow: number;
    charWidth?: number;
    // (undocumented)
    font?: string;
    labelGutter?: boolean;
    labelWidth?: LabelWidth;
    spacing?: HexSpacing;
}

// @public
export function linearRowPlan(byteLength: number, bytesPerRow: number): RowPlan;

// @public
export function measureCharWidth(font: string): number;

// @public
export class MemoryBinaryBuffer implements BinaryBuffer {
    constructor(data: Uint8Array | ArrayBuffer);
    // (undocumented)
    get length(): number;
    // (undocumented)
    read(offset: number, length: number): Uint8Array;
    // (undocumented)
    toUint8Array(): Uint8Array;
    // (undocumented)
    write(offset: number, bytes: Uint8Array): void;
}

// @public
export class MemoryByteSource extends AbstractByteSource {
    constructor(data: Uint8Array | ArrayBuffer);
    apply(changes: ChangeSet): void;
    ensure(): Promise<void>;
    get length(): number;
    peek(offset: number, length: number, out?: Uint8Array): Uint8Array | undefined;
    save(): AsyncGenerator<Uint8Array>;
    toUint8Array(): Uint8Array;
}

// @public
export const normalizedSelection: (anchor: number, head: number) => ByteSelection;

// @public
export class PagedByteSource extends AbstractByteSource {
    constructor(options: PagedByteSourceOptions);
    // (undocumented)
    ensure(offset: number, length: number, signal?: AbortSignal): Promise<void>;
    // (undocumented)
    readonly length: number;
    // (undocumented)
    peek(offset: number, length: number, out?: Uint8Array): Uint8Array | undefined;
    // (undocumented)
    get residentPages(): number;
}

// @public
export interface PagedByteSourceOptions {
    fetch: (offset: number, length: number, signal?: AbortSignal) => Promise<Uint8Array>;
    // (undocumented)
    length: number;
    maxPages?: number;
    // (undocumented)
    pageSize?: number;
}

// @public
export function parseBinding(text: string): Binding;

// @public
export function parseHexQuery(value: string): Uint8Array;

// @public
export interface Piece {
    // (undocumented)
    length: number;
    offset: number;
    // (undocumented)
    origin: PieceOrigin;
}

// @public
export type PieceOrigin = "original" | "added";

// @public
export class PieceTableSource extends AbstractByteSource {
    constructor(original: ByteSource | Uint8Array);
    apply(changes: ChangeSet): void;
    ensure(offset: number, length: number, signal?: AbortSignal): Promise<void>;
    get hasEdits(): boolean;
    get length(): number;
    peek(offset: number, length: number, out?: Uint8Array): Uint8Array | undefined;
    get pieceCount(): number;
    save(chunkSize?: number): AsyncGenerator<Uint8Array>;
}

// @public
export class PieceTree {
    constructor(pieces?: readonly Piece[]);
    // (undocumented)
    get bytes(): number;
    collect(offset: number, length: number, out: Piece[]): Piece[];
    // (undocumented)
    insert(offset: number, piece: Piece): void;
    // (undocumented)
    get pieceCount(): number;
    // (undocumented)
    remove(from: number, to: number): void;
    segments(offset: number, length: number): Generator<Piece>;
    toArray(): Piece[];
}

// @public
export type Platform = "mac" | "windows" | "linux";

// @public
export function platformFromName(name: string): Platform | undefined;

// @public
export type PrintableChar = (byte: number) => string;

// @public
export function printableTable(printable?: PrintableChar): readonly string[];

// @public (undocumented)
export function readFont(element: Element, fallback?: string): string;

// @public
export function readTheme(element: Element, fallback?: HexTheme): HexTheme;

// @public
export function readWhole(source: ByteSource, signal: AbortSignal | undefined): Promise<Uint8Array>;

// @public
export interface RenderRequest {
    // (undocumented)
    cursor: Cursor;
    decorations?: readonly Decoration[] | DecorationQuery;
    // (undocumented)
    display?: HexDisplayOptions;
    layout: HexLayout;
    plan?: RowPlan;
    // (undocumented)
    rowHeight: number;
    scrollLeft?: number;
    // (undocumented)
    scrollTop: number;
    // (undocumented)
    selection?: ByteSelection;
    // (undocumented)
    source: ByteSource;
    // (undocumented)
    theme?: HexTheme;
    // (undocumented)
    visibleRows: VisibleRows;
}

// @public
export interface ResolvedKeymap {
    bindings: readonly Keybinding[];
    lookup: readonly KeyLookupEntry[];
}

// @public
export type ResolvedSpacing = Required<HexSpacing>;

// @public
export function resolveKeymap(platform: Platform, overrides?: Keymap, text?: HexText): ResolvedKeymap;

// @public
export function resolvePlatform(platform?: Platform): Platform;

// @public
export function resolveText(overrides?: HexTextOverrides): HexText;

// @public
export const rollingHashInternals: {
    weakHash: typeof weakHash;
    roll: typeof roll;
};

// @public
export interface RowPlan {
    at(row: number): RowSpan;
    indexOf(offset: number): number;
    rowOf(offset: number): number;
    // (undocumented)
    readonly rows: number;
}

// @public
export function rowsIn(total: number, options: Pick<ViewportOptions, "rowHeight" | "height" | "scrollTop">): VisibleRows;

// @public
export interface RowSpan {
    // (undocumented)
    length: number;
    // (undocumented)
    offset: number;
}

// @public
export function scriptFor(before: Uint8Array, after: Uint8Array, options?: ScriptPlacement): HexDifference[];

// @public
export interface ScriptPlacement {
    leftOffset?: number;
    // (undocumented)
    limit?: number;
    // (undocumented)
    maxDistance?: number;
    // (undocumented)
    rightOffset?: number;
    // (undocumented)
    signal?: AbortSignal;
}

// @public
export interface ScrollScaleOptions {
    // (undocumented)
    maxScrollHeight?: number;
    viewportHeight?: number;
}

// @public
export type SearchDirection = "next" | "previous";

// @public
export const searchKind = "search";

// @public
export interface SearchMatch {
    // (undocumented)
    end: number;
    // (undocumented)
    start: number;
}

// @public
export const searchMatchLimit = 1000;

// @public
export type SearchMode = "hex" | "text" | (string & {});

// @public
export interface SearchOptions {
    // (undocumented)
    chunkSize?: number;
    // (undocumented)
    readAhead?: number;
    signal?: AbortSignal;
}

// @public
export interface SearchProvider {
    findAll?(source: ByteSource, query: string, mode: string, limit: number, signal?: AbortSignal): Promise<SearchMatch[]>;
    // (undocumented)
    findNext(source: ByteSource, query: string, mode: string, from: number, signal?: AbortSignal): Promise<SearchMatch | undefined>;
    // (undocumented)
    findPrevious(source: ByteSource, query: string, mode: string, before: number, signal?: AbortSignal): Promise<SearchMatch | undefined>;
    replacement?(match: SearchMatch, replacement: string, mode: string): Uint8Array;
}

// @public
export interface SearchRequest {
    // (undocumented)
    kind: "search" | "replace" | "goto";
}

// @public
export const substituteChar = ".";

// @public
export const themeProperties: Record<keyof HexTheme, string>;

// @public
export function throwIfAborted(signal: AbortSignal | undefined, message?: string): void;

// @public
export interface ViewportOptions {
    // (undocumented)
    bytesPerRow: number;
    // (undocumented)
    height: number;
    // (undocumented)
    rowHeight: number;
    // (undocumented)
    scrollTop: number;
}

// @public
export interface VisibleRows {
    // (undocumented)
    first: number;
    // (undocumented)
    last: number;
    // (undocumented)
    total: number;
}

// Warnings were encountered during analysis:
//
// dist/anchored-diff.d.ts:30:5 - (ae-forgotten-export) The symbol "weakHash" needs to be exported by the entry point index.d.ts
// dist/anchored-diff.d.ts:31:5 - (ae-forgotten-export) The symbol "roll" needs to be exported by the entry point index.d.ts

// (No @packageDocumentation comment for this package)

```
