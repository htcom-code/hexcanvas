## API Report File for "@hexcanvas/svelte"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import type { AddressRadix } from '@hexcanvas/core';
import type { BinaryBuffer } from '@hexcanvas/core';
import type { ByteGroupSize } from '@hexcanvas/core';
import type { ByteSource } from '@hexcanvas/core';
import type { EditMode } from '@hexcanvas/core';
import type { FeatureMode } from '@hexcanvas/core';
import { HexCanvasElement } from '@hexcanvas/element';
import type { HexEngine } from '@hexcanvas/core';
import type { HexSpacing } from '@hexcanvas/core';
import type { HexTextOverrides } from '@hexcanvas/core';
import type { HexTheme } from '@hexcanvas/core';
import type { Keymap } from '@hexcanvas/core';
import type { LabelWidth } from '@hexcanvas/core';
import type { Platform } from '@hexcanvas/core';
import type { SearchMode } from '@hexcanvas/core';
import type { SearchProvider } from '@hexcanvas/core';

// @public
export interface ActionReturn {
    destroy(): void;
    update(options: HexEditorOptions): void;
}

// @public
export function createHexEditor(container: Element, options: HexEditorOptions): HexCanvasElement;

export { HexCanvasElement }

// @public
export function hexEditor(node: HTMLElement, options: HexEditorOptions): ActionReturn;

// @public
export interface HexEditorOptions {
    addressRadix?: AddressRadix;
    asciiColumn?: boolean;
    byteGroup?: ByteGroupSize;
    bytesPerRow?: number;
    decorationLabels?: boolean;
    editMode?: EditMode;
    font?: string;
    goto?: FeatureMode;
    keymap?: Keymap;
    labelWidth?: LabelWidth;
    onEngine?: (engine: HexEngine) => void;
    platform?: Platform;
    replace?: FeatureMode;
    rowHeight?: number;
    search?: FeatureMode;
    searchModes?: readonly SearchMode[];
    searchProvider?: SearchProvider;
    source: ByteSource | BinaryBuffer;
    spacing?: HexSpacing;
    text?: HexTextOverrides;
    theme?: HexTheme;
}

// (No @packageDocumentation comment for this package)

```
